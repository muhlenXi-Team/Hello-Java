<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.muhlenxi.dao.IStudentDao">
    <!--查询所有-->
    <select id="findAll" resultType="com.muhlenxi.domain.Student">
        SELECT * FROM student;
    </select>

    <!--保存学生-->
    <insert id="saveStudent" parameterType="com.muhlenxi.domain.Student">
        <!--配置插入操作后，获取插入数据的id-->
        <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">
            select last_insert_id();
        </selectKey>
        INSERT INTO student
         (name, gender, age, addTime, math, english, school_id)
         values (#{name}, #{gender}, #{age}, #{addTime}, #{math}, #{english}, #{school_id});
    </insert>

    <!--删除学生-->
    <delete id="deleteStudent" parameterType="java.lang.Integer">
        DELETE FROM student where id = #{uid};
    </delete>

    <!--更改信息-->
    <update id="updateStudent" parameterType="com.muhlenxi.domain.Student">
        UPDATE student SET name=#{name}, gender=#{gender}, age=#{age}, math=#{math}, english=#{english} WHERE id=#{id};
    </update>

    <!--根据 id 查询学生-->
    <select id="findStudentById" parameterType="INT" resultType="com.muhlenxi.domain.Student">
        SELECT * FROM student WHERE id=#{uid};
    </select>

    <!--根据名字模糊查询-->
    <select id="findStudentsByName" parameterType="String" resultType="com.muhlenxi.domain.Student">
        SELECT * FROM student WHERE name like #{name};
    </select>

    <!--查询总记录条数-->
    <select id="findTotal" resultType="INT">
        SELECT COUNT(id) FROM student;
    </select>

</mapper>